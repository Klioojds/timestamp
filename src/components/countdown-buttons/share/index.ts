/**
 * Share Module
 * Components for sharing countdown URLs with timezone options.
 * 
 * @packageDocumentation
 */

// Types
export type { ShareController, ShareTargets } from './types';

// Factory
export { createShareControl } from './share-control';
